STAR ELECTRIC ENTERPRISES — logo assets
=======================================

star-electric-logo.jpg          1600 x 533  — horizontal lockup,
                                exactly as supplied. Kept untouched.
star-electric-logo-trimmed.png  1237 x 220  — the same horizontal
                                lockup with the surrounding white
                                space cropped away (content bounds
                                were 1563x278 inside the 1600x533
                                canvas). THIS is what the header and
                                footer display, so the wordmark reads
                                at a usable size. Artwork is identical
                                — only empty margin was removed.
star-electric-logo-stacked.jpg  1254 x 1254 — stacked lockup.
                                Used for og:image (social sharing).
favicon.png                     256 x 256   — the "SEE" mark, cropped
                                straight out of the stacked file.
                                Used as the browser tab icon.

All three are the supplied artwork. Nothing has been redrawn or recreated.

Colours sampled from star-electric-logo.jpg and used across the site:
    navy indigo  #221B66   (mark + wordmark fill)
    navy shade   #1D1858
    outline      #080528
    signal red   #FD2A2A   (the red "E" stroke)

If a transparent PNG or vector (SVG/AI/EPS) version becomes available,
drop it in here and update the LOGO constant near the top of
js/components.js — the header and footer both read from that one value.
